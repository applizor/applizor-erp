version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: applizor-postgres
    environment:
      POSTGRES_USER: applizor
      POSTGRES_PASSWORD: applizor123
      POSTGRES_DB: applizor_erp
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U applizor" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - applizor-network

  # Backend API (Node.js)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: applizor-backend-dev
    env_file:
      - ./backend/.env.development
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://applizor:applizor123@postgres:5432/applizor_erp?schema=public
      PORT: 5000
      FRONTEND_URL: http://localhost:3000
      REDIS_URL: redis://redis:6379
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - /app/node_modules
      - ./backend/uploads:/app/uploads
    command: node dist/src/server.js
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - applizor-network

  # Gotenberg for PDF generation
  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: applizor-gotenberg
    ports:
      - "8000:3000"
    networks:
      - applizor-network

  # Frontend (Next.js)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: applizor-frontend-dev
    environment:
      NODE_ENV: development
      NEXT_PUBLIC_API_URL: http://localhost:5000/api
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run start
    depends_on:
      - backend
    networks:
      - applizor-network

  # Redis for Background Queues
  redis:
    image: redis:7-alpine
    container_name: applizor-redis
    ports:
      - "6379:6379"
    networks:
      - applizor-network

volumes:
  postgres_data:


networks:
  applizor-network:
    driver: bridge
